#!/usr/bin/env node

var app = require('../app');

app.set('port', process.env.PORT || app.conf.get('port'));

var server = app.listen(app.get('port'), function() {
  console.log('Freight server listening on port ' + server.address().port);
});
